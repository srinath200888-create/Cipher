---
title: Windows (WSL)
description: Uruchamiaj Cipher na Windows z WSL dla najlepszych efektow.
---

import { Steps } from "@astrojs/starlight/components"

Chociaz Cipher moze dzialac bezposrednio na Windows, dla najlepszych rezultatow zalecamy [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install). WSL zapewnia srodowisko Linuksa, ktore bardzo dobrze wspolpracuje z funkcjami Cipher.

:::tip[Dlaczego WSL?]
WSL oferuje lepsza wydajnosc systemu plikow, pelna obsluge terminala i zgodnosc z narzedziami developerskimi, na ktorych opiera sie Cipher.
:::

---

## Konfiguracja

<Steps>

1. **Zainstaluj WSL**

   Jesli jeszcze tego nie zrobiles, [zainstaluj WSL](https://learn.microsoft.com/en-us/windows/wsl/install) wedlug oficjalnego przewodnika Microsoft.

2. **Zainstaluj Cipher w WSL**

   Po skonfigurowaniu WSL otworz terminal WSL i zainstaluj Cipher jedna z [metod instalacji](/docs/).

   ```bash
   curl -fsSL https://opencode.ai/install | bash
   ```

3. **Korzystaj z Cipher przez WSL**

   Przejdz do katalogu projektu (pliki Windows sa dostepne przez `/mnt/c/`, `/mnt/d/` itd.) i uruchom Cipher.

   ```bash
   cd /mnt/c/Users/YourName/project
   Cipher
   ```

</Steps>

---

## Aplikacja desktopowa + serwer WSL

Jesli wolisz aplikacje desktopowa Cipher, ale chcesz uruchamiac serwer w WSL:

1. **Uruchom serwer w WSL** z `--hostname 0.0.0.0`, aby zezwolic na polaczenia zewnetrzne:

   ```bash
   Cipher serve --hostname 0.0.0.0 --port 4096
   ```

2. **Polacz aplikacje desktopowa** z `http://localhost:4096`

:::note
Jesli `localhost` nie dziala w Twojej konfiguracji, polacz sie przez adres IP WSL (w WSL: `hostname -I`) i uzyj `http://<wsl-ip>:4096`.
:::

:::caution
Przy uzyciu `--hostname 0.0.0.0` ustaw `CIPHER_SERVER_PASSWORD`, aby zabezpieczyc serwer.

```bash
CIPHER_SERVER_PASSWORD=your-password Cipher serve --hostname 0.0.0.0
```

:::

---

## Klient web + WSL

Dla najlepszej pracy w przegladarce na Windows:

1. **Uruchamiaj `Cipher web` w terminalu WSL**, a nie w PowerShell:

   ```bash
   Cipher web --hostname 0.0.0.0
   ```

2. **Otworz w przegladarce Windows** pod adresem `http://localhost:<port>` (Cipher wypisze URL)

Uruchamianie `Cipher web` z WSL zapewnia poprawny dostep do systemu plikow i integracje z terminalem, a nadal pozostaje dostepne z przegladarki Windows.

---

## Dostep do plikow Windows

WSL moze uzyskac dostep do wszystkich plikow Windows przez katalog `/mnt/`:

- Dysk `C:` → `/mnt/c/`
- Dysk `D:` → `/mnt/d/`
- I tak dalej...

Przyklad:

```bash
cd /mnt/c/Users/YourName/Documents/project
Cipher
```

:::tip
Aby uzyskac najplynniejsza prace, rozwaz sklonowanie/skopiowanie repozytorium do systemu plikow WSL (np. do `~/code/`) i uruchamianie tam Cipher.
:::

---

## Wskazowki

- Uruchamiaj Cipher w WSL dla projektow zapisanych na dyskach Windows - dostep do plikow jest bezproblemowy
- Uzywaj [rozszerzenia WSL dla VS Code](https://code.visualstudio.com/docs/remote/wsl) razem z Cipher, aby miec zintegrowany workflow
- Konfiguracja Cipher i sesje sa przechowywane w srodowisku WSL pod `~/.local/share/opencode/`
