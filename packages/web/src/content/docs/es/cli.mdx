---
title: CLI
description: Opciones y comandos de la CLI de Cipher.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI de Cipher de forma predeterminada inicia el [TUI](/docs/tui) cuando se ejecuta sin ningún argumento.

```bash
Cipher
```

Pero también acepta comandos como se documenta en esta página. Esto le permite interactuar con Cipher mediante programación.

```bash
Cipher run "Explain how closures work in JavaScript"
```

---

### tui

Inicie la interfaz de usuario del terminal Cipher.

```bash
Cipher [project]
```

#### Opciones

| Opción                                   | Corta | Descripción                                                           |
| ---------------------------------------- | ----- | --------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar la última sesión                                            |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID de sesión para continuar                                           |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Aviso de uso                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                        |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a utilizar                                                     |
| <nobr><code>{"--port"}</code></nobr>     |       | Puerto para escuchar                                                  |
| <nobr><code>{"--hostname"}</code></nobr> |       | Nombre de host para escuchar                                          |

---

## Comandos

La CLI de Cipher también tiene los siguientes comandos.

---

### agent

Administrar agentes para Cipher.

```bash
Cipher agent [command]
```

---

### attach

Conecte una terminal a un servidor backend Cipher que ya se esté ejecutando y iniciado mediante los comandos `serve` o `web`.

```bash
Cipher attach [url]
```

Esto permite usar TUI con un backend remoto Cipher. Por ejemplo:

```bash
# Start the backend server for web/mobile access
Cipher web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
Cipher attach http://10.20.30.40:4096
```

#### Opciones

| Opción                                   | Corta | Descripción                                                                               |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directorio de trabajo para iniciar TUI en                                                 |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar la última sesión                                                                |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID de sesión para continuar                                                               |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`)                     |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Contraseña de autenticación básica (predeterminada: `CIPHER_SERVER_PASSWORD`)           |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuario de autenticación básica (predeterminado: `CIPHER_SERVER_USERNAME` u `Cipher`) |

---

#### create

Cree un nuevo agente con configuración personalizada.

```bash
Cipher agent create
```

Este comando lo guiará en la creación de un nuevo agente con un mensaje del sistema personalizado y una configuración de herramientas.

---

#### list

Enumere todos los agentes disponibles.

```bash
Cipher agent list
```

---

### auth

Comando para administrar credenciales e iniciar sesión para proveedores.

```bash
Cipher auth [command]
```

---

#### login

Cipher funciona con la lista de proveedores en [Models.dev](https://models.dev), por lo que puede usar `Cipher auth login` para configurar las claves API para cualquier proveedor que desee utilizar. Esto se almacena en `~/.local/share/opencode/auth.json`.

```bash
Cipher auth login
```

Cuando se inicia Cipher, carga los proveedores desde el archivo de credenciales. Y si hay claves definidas en sus entornos o un archivo `.env` en su proyecto.

---

#### list

Enumera todos los proveedores autenticados tal como están almacenados en el archivo de credenciales.

```bash
Cipher auth list
```

O la versión corta.

```bash
Cipher auth ls
```

---

#### logout

Cierra tu sesión de un proveedor eliminándolo del archivo de credenciales.

```bash
Cipher auth logout
```

---

### github

Administre el agente GitHub para la automatización del repositorio.

```bash
Cipher github [command]
```

---

#### install

Instale el agente GitHub en su repositorio.

```bash
Cipher github install
```

Esto configura el flujo de trabajo de acciones GitHub necesario y lo guía a través del proceso de configuración. [Más información](/docs/github).

---

#### run

Ejecute el agente GitHub. Esto se usa normalmente en acciones GitHub.

```bash
Cipher github run
```

##### Opciones

| Opción                                | Descripción                                    |
| ------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub evento simulado para ejecutar el agente |
| <nobr><code>{"--token"}</code></nobr> | GitHub token de acceso personal                |

---

### mcp

Administrar servidores de protocolo de contexto modelo.

```bash
Cipher mcp [command]
```

---

#### add

Agregue un servidor MCP a su configuración.

```bash
Cipher mcp add
```

Este comando lo guiará para agregar un servidor MCP local o remoto.

---

#### list

Enumere todos los servidores MCP configurados y su estado de conexión.

```bash
Cipher mcp list
```

O utilice la versión corta.

```bash
Cipher mcp ls
```

---

#### auth

Autentíquese con un servidor MCP habilitado para OAuth.

```bash
Cipher mcp auth [name]
```

Si no proporciona un nombre de servidor, se le pedirá que seleccione entre los servidores compatibles con OAuth disponibles.

También puede enumerar los servidores compatibles con OAuth y su estado de autenticación.

```bash
Cipher mcp auth list
```

O utilice la versión corta.

```bash
Cipher mcp auth ls
```

---

#### logout

Elimine las credenciales OAuth para un servidor MCP.

```bash
Cipher mcp logout [name]
```

---

#### debug

Depurar problemas de conexión OAuth para un servidor MCP.

```bash
Cipher mcp debug <name>
```

---

### models

Enumere todos los modelos disponibles de los proveedores configurados.

```bash
Cipher models [provider]
```

Este comando muestra todos los modelos disponibles en sus proveedores configurados en el formato `provider/model`.

Esto es útil para determinar el nombre exacto del modelo que se usará en [su configuración](/docs/config/).

Opcionalmente, puede pasar un ID de proveedor para filtrar modelos por ese proveedor.

```bash
Cipher models anthropic
```

#### Opciones

| Opción                                  | Descripción                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Actualizar la caché de modelos desde models.dev                             |
| <nobr><code>{"--verbose"}</code></nobr> | Utilice una salida del modelo más detallada (incluye metadatos como costos) |

Utilice el indicador `--refresh` para actualizar la lista de modelos almacenados en caché. Esto es útil cuando se han agregado nuevos modelos a un proveedor y desea verlos en Cipher.

```bash
Cipher models --refresh
```

---

### run

Ejecute Cipher en modo no interactivo pasando un mensaje directamente.

```bash
Cipher run [message..]
```

Esto es útil para secuencias de comandos, automatización o cuando desea una respuesta rápida sin iniciar el TUI completo. Por ejemplo.

```bash "Cipher run"
Cipher run Explain the use of context in Go
```

También puede conectarse a una instancia `Cipher serve` en ejecución para evitar tiempos de arranque en frío del servidor MCP en cada ejecución:

```bash
# Start a headless server in one terminal
Cipher serve

# In another terminal, run commands that attach to it
Cipher run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opciones

| Opción                                   | Corta | Descripción                                                                               |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | El comando a ejecutar, use mensaje para args                                              |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar la última sesión                                                                |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID de sesión para continuar                                                               |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`)                     |
| <nobr><code>{"--share"}</code></nobr>    |       | Comparte la sesión                                                                        |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                                            |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a utilizar                                                                         |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | Archivo(s) para adjuntar al mensaje                                                       |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: predeterminado (formateado) o json (eventos JSON sin formato)                    |
| <nobr><code>{"--title"}</code></nobr>    |       | Título de la sesión (utiliza un mensaje truncado si no se proporciona ningún valor)       |
| <nobr><code>{"--attach"}</code></nobr>   |       | Adjuntar a un servidor Cipher en ejecución (por ejemplo, http://localhost:4096)         |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Contraseña de autenticación básica (predeterminada: `CIPHER_SERVER_PASSWORD`)           |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuario de autenticación básica (predeterminado: `CIPHER_SERVER_USERNAME` u `Cipher`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directorio de ejecución, o ruta en el servidor remoto al adjuntar                         |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante del modelo (esfuerzo de razonamiento específico del proveedor)                   |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostrar bloques de pensamiento                                                            |
| <nobr><code>{"--port"}</code></nobr>     |       | Puerto para el servidor local (el puerto predeterminado es aleatorio)                     |

---

### serve

Inicie un servidor Cipher sin cabeza para acceso API. Consulte los [documentos del servidor](/docs/server) para conocer la interfaz HTTP completa.

```bash
Cipher serve
```

Esto inicia un servidor HTTP que proporciona acceso API a la funcionalidad Cipher sin la interfaz TUI. Configure `CIPHER_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `Cipher`).

#### Opciones

| Opción                                   | Descripción                                          |
| ---------------------------------------- | ---------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Puerto para escuchar                                 |
| <nobr><code>{"--hostname"}</code></nobr> | Nombre de host para escuchar                         |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar el descubrimiento de mDNS                  |
| <nobr><code>{"--cors"}</code></nobr>     | Orígenes de navegador adicionales para permitir CORS |

---

### session

Administrar Cipher sesiones.

```bash
Cipher session [command]
```

---

#### list

Enumere todas las sesiones Cipher.

```bash
Cipher session list
```

##### Opciones

| Opción                                    | Corta | Descripción                             |
| ----------------------------------------- | ----- | --------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limitar a N sesiones más recientes      |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato de salida: tabla o json (tabla) |

---

### stats

Muestre el uso de tokens y las estadísticas de costos para sus sesiones Cipher.

```bash
Cipher stats
```

#### Opciones

| Opción                                  | Descripción                                                                                                              |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--days"}</code></nobr>    | Mostrar estadísticas de los últimos N días (todo el tiempo)                                                              |
| <nobr><code>{"--tools"}</code></nobr>   | Número de herramientas para mostrar (todas)                                                                              |
| <nobr><code>{"--models"}</code></nobr>  | Mostrar el desglose del uso del modelo (oculto de forma predeterminada). Pase un número para mostrar la parte superior N |
| <nobr><code>{"--project"}</code></nobr> | Filtrar por proyecto (todos los proyectos, cadena vacía: proyecto actual)                                                |

---

### export

Exportar datos de la sesión como JSON.

```bash
Cipher export [sessionID]
```

Si no proporciona una ID de sesión, se le pedirá que seleccione entre las sesiones disponibles.

---

### import

Importe datos de sesión desde un archivo JSON o una URL compartida Cipher.

```bash
Cipher import <file>
```

Puede importar desde un archivo local o una URL compartida Cipher.

```bash
Cipher import session.json
Cipher import https://opncd.ai/s/abc123
```

---

### web

Inicie un servidor Cipher sin cabeza con una interfaz web.

```bash
Cipher web
```

Esto inicia un servidor HTTP y abre un navegador web para acceder a Cipher a través de una interfaz web. Configure `CIPHER_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `Cipher`).

#### Opciones

| Opción                                   | Descripción                                          |
| ---------------------------------------- | ---------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Puerto para escuchar                                 |
| <nobr><code>{"--hostname"}</code></nobr> | Nombre de host para escuchar                         |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar el descubrimiento de mDNS                  |
| <nobr><code>{"--cors"}</code></nobr>     | Orígenes de navegador adicionales para permitir CORS |

---

### acp

Inicie un servidor ACP (Agent Client Protocol).

```bash
Cipher acp
```

Este comando inicia un servidor ACP que se comunica a través de stdin/stdout usando nd-JSON.

#### Opciones

| Opción                                   | Descripción                  |
| ---------------------------------------- | ---------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Directorio de trabajo        |
| <nobr><code>{"--port"}</code></nobr>     | Puerto para escuchar         |
| <nobr><code>{"--hostname"}</code></nobr> | Nombre de host para escuchar |

---

### uninstall

Desinstale Cipher y elimine todos los archivos relacionados.

```bash
Cipher uninstall
```

#### Opciones

| Opción                                      | Corta | Descripción                               |
| ------------------------------------------- | ----- | ----------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Mantener archivos de configuración        |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Conservar datos de sesión e instantáneas  |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostrar lo que se eliminaría sin eliminar |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Saltar mensajes de confirmación           |

---

### upgrade

Actualiza Cipher a la última versión o a una versión específica.

```bash
Cipher upgrade [target]
```

Para actualizar a la última versión.

```bash
Cipher upgrade
```

Para actualizar a una versión específica.

```bash
Cipher upgrade v0.1.48
```

#### Opciones

| Opción                                 | Corta | Descripción                                                         |
| -------------------------------------- | ----- | ------------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | El método de instalación que se utilizó; curl, npm, pnpm, bun, brew |

---

## Opciones globales

La CLI de Cipher toma las siguientes banderas globales.

| Opción                                     | Corta | Descripción                                  |
| ------------------------------------------ | ----- | -------------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Mostrar ayuda                                |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Número de versión de impresión               |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Imprimir registros en stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Nivel de registro (DEBUG, INFO, WARN, ERROR) |

---

## Variables de entorno

Cipher se puede configurar mediante variables de entorno.

| Variable                              | Type     | Description                                                                     |
| ------------------------------------- | -------- | ------------------------------------------------------------------------------- |
| `CIPHER_AUTO_SHARE`                 | booleano | Compartir sesiones automáticamente                                              |
| `CIPHER_GIT_BASH_PATH`              | cadena   | Ruta al ejecutable de Git Bash en Windows                                       |
| `CIPHER_CONFIG`                     | cadena   | Ruta al archivo de configuración                                                |
| `CIPHER_CONFIG_DIR`                 | cadena   | Ruta al directorio de configuración                                             |
| `CIPHER_CONFIG_CONTENT`             | cadena   | Contenido de configuración json en línea                                        |
| `CIPHER_DISABLE_AUTOUPDATE`         | booleano | Deshabilitar las comprobaciones automáticas de actualizaciones                  |
| `CIPHER_DISABLE_PRUNE`              | booleano | Deshabilitar la poda de datos antiguos                                          |
| `CIPHER_DISABLE_TERMINAL_TITLE`     | booleano | Deshabilitar las actualizaciones automáticas de títulos de terminal             |
| `CIPHER_PERMISSION`                 | cadena   | Configuración de permisos json incorporados                                     |
| `CIPHER_DISABLE_DEFAULT_PLUGINS`    | booleano | Deshabilitar complementos predeterminados                                       |
| `CIPHER_DISABLE_LSP_DOWNLOAD`       | booleano | Deshabilitar las descargas automáticas del servidor LSP                         |
| `CIPHER_ENABLE_EXPERIMENTAL_MODELS` | booleano | Habilitar modelos experimentales                                                |
| `CIPHER_DISABLE_AUTOCOMPACT`        | booleano | Deshabilitar la compactación automática de contexto                             |
| `CIPHER_DISABLE_CLAUDE_CODE`        | booleano | Deshabilitar la lectura desde `.claude` (mensaje + habilidades)                 |
| `CIPHER_DISABLE_CLAUDE_CODE_PROMPT` | booleano | Desactivar lectura `~/.claude/CLAUDE.md`                                        |
| `CIPHER_DISABLE_CLAUDE_CODE_SKILLS` | booleano | Deshabilitar la carga `.claude/skills`                                          |
| `CIPHER_DISABLE_MODELS_FETCH`       | booleano | Deshabilitar la recuperación de modelos desde fuentes remotas                   |
| `CIPHER_FAKE_VCS`                   | cadena   | Proveedor de VCS falso para fines de prueba                                     |
| `CIPHER_CLIENT`                     | cadena   | Identificador de cliente (por defecto `cli`)                                    |
| `CIPHER_ENABLE_EXA`                 | booleano | Habilitar las herramientas de búsqueda web de Exa                               |
| `CIPHER_SERVER_PASSWORD`            | cadena   | Habilite la autenticación básica para `serve`/`web`                             |
| `CIPHER_SERVER_USERNAME`            | cadena   | Anular el nombre de usuario de autenticación básica (predeterminado `Cipher`) |
| `CIPHER_MODELS_URL`                 | cadena   | URL personalizada para buscar la configuración de modelos                       |

---

### Experimental

Estas variables de entorno habilitan funciones experimentales que pueden cambiar o eliminarse.

| Variable                                        | Type     | Description                                                |
| ----------------------------------------------- | -------- | ---------------------------------------------------------- |
| `CIPHER_EXPERIMENTAL`                         | booleano | Habilitar funciones experimentales de la bandera global    |
| `CIPHER_EXPERIMENTAL_ICON_DISCOVERY`          | booleano | Habilitar descubrimiento de íconos                         |
| `CIPHER_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | booleano | Deshabilitar copia al seleccionar en TUI                   |
| `CIPHER_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | número   | Tiempo de espera predeterminado para comandos bash en ms   |
| `CIPHER_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | número   | Tokens de salida máximos para respuestas LLM               |
| `CIPHER_EXPERIMENTAL_FILEWATCHER`             | booleano | Habilite el observador de archivos para todo el directorio |
| `CIPHER_EXPERIMENTAL_OXFMT`                   | booleano | Habilitar el formateador oxfmt                             |
| `CIPHER_EXPERIMENTAL_LSP_TOOL`                | booleano | Habilitar herramienta experimental LSP                     |
| `CIPHER_EXPERIMENTAL_DISABLE_FILEWATCHER`     | booleano | Deshabilitar el observador de archivos                     |
| `CIPHER_EXPERIMENTAL_EXA`                     | booleano | Habilitar funciones experimentales de Exa                  |
| `CIPHER_EXPERIMENTAL_LSP_TY`                  | booleano | Habilitar Habilitar TY LSP para archivos python            |
| `CIPHER_EXPERIMENTAL_PLAN_MODE`               | booleano | Habilitar modo de plan                                     |
| `CIPHER_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | booleano | Habilitar tareas de subagentes en segundo plano            |
| `CIPHER_EXPERIMENTAL_EVENT_SYSTEM`            | booleano | Habilitar sistema de eventos experimental                  |
| `CIPHER_EXPERIMENTAL_NATIVE_LLM`              | booleano | Habilitar ruta nativa de solicitud LLM                     |
| `CIPHER_EXPERIMENTAL_PARALLEL`                | booleano | Habilitar ejecución paralela de búsqueda web               |
| `CIPHER_EXPERIMENTAL_SCOUT`                   | booleano | Habilitar subagente Scout                                  |
| `CIPHER_EXPERIMENTAL_WORKSPACES`              | booleano | Habilitar soporte de espacios de trabajo                   |
